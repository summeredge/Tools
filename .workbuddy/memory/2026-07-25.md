# 2026-07-25

## 修复 markdown 工具代码错误
- `src/main.ts` 的 `escapeHtml` 字符映射被损坏：`"` 键的值变成 `"""`（语法错误，导致整个项目无法编译/构建），且 `& < >` 未转义（XSS + 数据丢失风险）。已修正为标准实体映射 `&amp; &lt; &gt; &quot; &#39;`。
- Markdown 工具栏 `codeblock` 按钮（renderToolContent 模板字符串内）含字面反引号 ```` ``` ````，提前闭合了外层模板字符串，造成语法错误。已用 `\`` 转义。
- 验证：`tsc --noEmit` 通过、`vitest run` 3/3 通过、`vite build` 成功（28 模块）。

## 推送修复到 GitHub（含 .git 损坏修复）
- 远程 `summeredge/Tools` 的 `main` 此前虽有几个 "fix" 提交，但内容仍未真正修复（escapeHtml 仍是 `""` 空值、codeblock 仍是未转义反引号），且本地 .git 在 `git pull --rebase` 时被破坏：`.git/refs` 被清空、`.git/objects` 缺失对象（如 `110e5b57`、`375cbce`）。
- 修复本地仓库：清空 refs/index 后从完整克隆 `git fetch` 拉取完整 pack 修复对象库，再把 `main` 指向远程 tip 并 `reset --hard` 同步工作树。
- 重新应用两处修复后提交 `70b55c7`，经 SSH（`git@github.com:summeredge/Tools.git`）推送成功（`644623e..70b55c7`），远程 `main` 已确认 = `70b55c7`。
- 注意：本环境沙箱对 git 的 ref 写入存在跨进程文件系统视图不一致；HTTPS 推送因无凭据失败，改用 SSH 一次性 URL 推送；remote 最终保留为用户指定的 HTTPS 地址，并补建 `origin/main` 跟踪与 upstream。
- 验证：`git status` 显示与 `origin/main` 同步，本地/远程 `main` 均为 `70b55c7`。
