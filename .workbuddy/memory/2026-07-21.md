# 2026-07-21

## 过程工程工具四阶段改造（summeredge/Tools）

完成基线（24 测试）→ 前置 IF97 共享层 → 阶段1 管道 → 阶段2 控制阀 → 阶段3 换热器 → 阶段4 蒸汽 PRDS → 源码约束测试。最终 96 测试全过，build/lint 绿。

关键决策：
- IF97 唯一数值实现：`src/tools/shared/if97-vendor.ts`，由 `scripts/extract-if97.cjs` 从 `public/tools/if97.html` 内嵌的 iapws-if97 v2.1.5 (MIT) 打包体逐字节提取（IIFE 内部 + 显式导出）。`if97-adapter.ts` 提供中文相态判断、饱和性质、P-h 割线反算、Sutherland 空气黏度、真实气体密度。旧 if97.html 保持原样不动。
- 饱和液/汽性质用 `solvePT(p, Tsat±0.02K)` 两侧单相求解（bundle 未暴露 Region 4 方程）。
- 管道：保留原 Darcy/Colebrook/Swamee-Jain 核心；新增 fluid 字段（water/air/steam/custom），气体走等温低压降平均密度迭代（<1e-6，50 次）。
- 控制阀：IEC 60534-2-1，Kv = Q√(G/ΔP)；气体 Kv = Qn/(257·Fp·Y)√(M·T1·Z/(ΔP(P1+P2)))；阻塞状态三态 yes/no/not-evaluated。
- 换热器只加验证不改核心：热侧出口>入口、冷侧出口<入口时不算 LMTD。
- steam-prds：三模式，节流 h2=h1；喷水能量平衡 ms·h1+mw·hw=(ms+mw)·hout；Spirax Sarco 案例（10000 kg/h、10 bar(a)、300°C、喷水 150°C、目标 185°C → mw≈1209 kg/h）。
- 注意：阶段 4 期间被插入了一个外部提交 f9d71b9 "update1"（steam-prds 初版），最终代码以 HEAD 为准，工作区干净。
- localStorage key：workbench:pipe-pressure-drop / workbench:control-valve-sizing / workbench:heat-exchanger / workbench:steam-prds（旧 key 未改）。
